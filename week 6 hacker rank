import re

def transform(match):
    if match.group(0) == '&&':
        return 'and'
    else:
        return 'or'

n = int(input())

for _ in range(n):
    line = input()

    
    pattern_and = r'(?<= )&&(?= )'
    pattern_or = r'(?<= )\|\|(?= )'
    
    line = re.sub(pattern_and, 'and', line)
    line = re.sub(pattern_or, 'or', line
    line = re.sub(pattern_and, 'and', line)
    line = re.sub(pattern_or, 'or', line)
    
    print(line)
